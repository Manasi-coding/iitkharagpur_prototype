# DataForge — Python verification/build tooling only.
# The shipped submission is vanilla JS; nothing here is imported by the app.

# Person 1 (write, similarity, injectNoise) and Person 2 (retrieveIterative):
# prototype/verify matrix math in Python before porting to JS
numpy>=1.26

# Person 1, Person 2, Person 3 (all core owners):
# verification-gate tests — hand-computed checks, convergence counts,
# correlation thresholds, crossover comparisons
pytest>=8.0

# Person 3: sanity-plot degradation/capacity curves (classical vs sparse)
# before committing to JS chart code
matplotlib>=3.8

# Person 3: automate capturing the 3 required [PRECOMPUTED] screenshots
# for the failure gallery (overload, correlated patterns, spurious output)
playwright>=1.45
